<!DOCTYPE html>
<html lang="<%= language %>">
<head>
  <meta charset="UTF-8" />
  <style>
    body { font-family: Arial, sans-serif; color: #333; }
    .header { text-align: center; }
    .logo { width: 100px; margin-bottom: 10px; }
    .content { padding: 20px; }
  </style>
</head>
<body>
  <div class="header">
    <img src="<%= logoUrl %>" alt="CMD Logo" class="logo" />
    <h2><%= language === 'fr' ? 'Bienvenue à CMD Football' : 'Welcome to CMD Football' %></h2>
  </div>
  <div class="content">
    <p>
      <%= language === 'fr'
        ? `Bonjour ${coachName}, nous sommes ravis de vous accueillir.`
        : `Hello ${coachName}, we’re excited to welcome you.` %>
    </p>
    <p>
      <%= language === 'fr'
        ? 'Accédez à votre tableau de bord ici :'
        : 'Access your dashboard here:' %>
    </p>
    <a href="<%= dashboardLink %>"><%= dashboardLink %></a>
  </div>
</body>
</html>